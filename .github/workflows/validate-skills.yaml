name: validate-skills

on:
  push:

jobs:
  validate-skills:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v8.1.0

      - name: Validate SKILL.md frontmatter against the Agent Skills spec
        run: uv run scripts/validate_skills.py
